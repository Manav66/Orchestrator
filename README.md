# flowctl

A small, local Python task orchestrator: define pipelines of dependent tasks,
run them with automatic retries and parallel execution, track history in
SQLite, trigger them via CLI or a schedule, and monitor everything through a
lightweight web dashboard.

> Work in progress. This README will be expanded with full usage docs,
> an architecture diagram, and a scoping explanation in a later phase.
